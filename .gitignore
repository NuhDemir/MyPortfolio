# ───────────── OS TARAFINDAN OLUŞTURULANLAR ─────────────
.DS_Store
Thumbs.db

# ───────────── LOG DOSYALARI ─────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ───────────── EDITÖR VE IDE AYARLARI ─────────────
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# ───────────── NODE MODULES (GENEL KURAL) ─────────────
node_modules/
# veya belirli alt dizinlerdeki node_modules:
# backend/node_modules/
# frontend/node_modules/
# myportfolio/node_modules/

# ───────────── ENV DOSYALARI (GİZLİ TUTULMALI) ─────────────
.env
.env.*
backend/.env
backend/.env.*
frontend/.env
frontend/.env.*
myportfolio/.env
myportfolio/.env.*

# ───────────── BUILD / DERLEME ÇIKTILARI ─────────────
build/
dist/
backend/build/
backend/dist/
frontend/build/
frontend/dist/
myportfolio/build/
myportfolio/dist/

# ───────────── VITE CACHE (Eğer Vite kullanıyorsan) ─────────────
.vite/
myportfolio/.vite/
frontend/.vite/

# ───────────── NEXT.js (Kullanıyorsan) ─────────────
.next/

# ───────────── TEMPORARY / GEÇİCİ DOSYALAR ─────────────
*.tmp
*.temp
*.swp
*.swo

# ───────────── TEST VE COVERAGE ÇIKTILARI ─────────────
coverage/
.nyc_output/
test-output/
cypress/videos/
cypress/screenshots/

# ───────────── NPM/YARN/PNPM CACHE VE AYARLAR ─────────────
.npm/
.yarn/cache/
.yarn/plugnplay/
.yarn/unplugged/
.pnpm-store/
.yarnrc.yml
.pnpm-debug.log

# ───────────── VERITABANI, BACKUP VE LOCAL DOSYALAR ─────────────
*.sqlite
*.sqlite3
*.db
*.bak
*.sql
dump.sql

# ───────────── SYSTEM / MAC-SPECIFIC ─────────────
.AppleDouble
.LSOverride
Icon?
ehthumbs.db
Desktop.ini

# ───────────── OUTPUT / EXPORT / ZIP DOSYALARI ─────────────
*.zip
*.tar
*.gz
*.rar

# ───────────── FIREBASE / ANALYTICS / ANALOG DOSYALAR (varsa) ─────────────
firebase-debug.log
firebase.json

# ───────────── PUBLIC (EĞER SADECE BUILD VARSA, RAW PUBLIC DİZİNİNİ DEĞİL) ─────────────
public/static/

# ───────────── PM2 LOGS (Node.js process yöneticisi kullanıyorsan) ─────────────
pids/
*.pid
*.seed
*.pid.lock

# ───────────── STORYBOOK / DOCS / DOCUSAURUS GİBİ ARAÇLAR ─────────────
.out/
storybook-static/
docs/.vitepress/dist/
